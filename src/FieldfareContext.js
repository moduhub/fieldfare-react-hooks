import { createContext } from 'react';

export const EnvironmentContext = createContext(undefined);
export const CollectionContext = createContext(undefined);
export const ServiceDescriptorContext = createContext(undefined);
export const LocalServiceContext = createContext(undefined);
export const RemoteServiceContext = createContext(undefined);
export const SelectedHostsContext = createContext(undefined);